# Copy this file to `functions/.env` for deployed Functions.
# Use `functions/.env.local` only for the local emulator override.
APP_URL=
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
WHOOP_REDIRECT_URI=
DUPR_EMAIL=
DUPR_PASSWORD=
TOKEN_ENCRYPTION_KEY=
